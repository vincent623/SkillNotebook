# Examples

This folder holds sample project roots and example skill packages used during local development.

The default development bootstrap points at:

- `examples/project-root`
  - skills live under `examples/project-root/.skills/`
